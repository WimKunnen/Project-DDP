.text

.global mont_add
.func mont_add, mont_add
.type mont_add, %function

cmp r2, #0
beq end_add_loop

add_loop:
	ldr r1, [r0], r1
	push {r4}
	add r4, r2, r1
	lsr r2, r4, #32
	str r4, [r0], r1
	pop {r4}
	subs r1, r1, #1
	bne add_loop

end_add_loop:

.endfunc
